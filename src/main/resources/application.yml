spring:
    config:
        import: security-variable.yml
    application:
        name: Memo
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/memo_ryu
        username: root
        password: ${mysql.password}
    thymeleaf:
        cache: false
    mvc:
        static-path-pattern: /static/**